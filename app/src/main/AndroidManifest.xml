<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.peopleofandroido.chillaxingcat">

    <application
        android:name=".ChillaxingCatApplication"
        android:allowBackup="true"
        android:icon="@drawable/splash_cat"
        android:label="@string/app_name"
        android:roundIcon="@drawable/splash_cat"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.ChillaxingCat"
        tools:targetApi="m">
        <activity android:name=".presentation.MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
    <uses-permission android:name="android.permission.INTERNET"/>
</manifest>